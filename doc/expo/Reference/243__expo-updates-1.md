# Expo Updates Protocol Version 1 协议规范

## 文档解决的问题

这篇文档定义了 **Expo Updates 协议第 1 版** 的完整技术规范。它描述了 Expo 应用如何从服务器获取 OTA（Over-The-Air，空中下发）更新的通信协议，包括请求格式、响应格式、资源（asset）下载流程以及密码学签名验证机制。

**为什么需要关注这个协议？** 在 React Web 开发中，部署更新只需将新文件推送到服务器，用户刷新浏览器即可加载。但在移动端，应用是通过应用商店分发的原生二进制包，用户不一定会及时更新 App。Expo Updates 提供了一种机制，可以在不重新发布应用商店版本的前提下，向已安装的 App 推送 JavaScript 代码和资源更新。这篇协议文档就是定义这个推送过程"客户端和服务器之间怎么对话"的技术标准。

**适用场景：**
- 你正在使用 `expo-updates` 库为 Expo 项目配置 OTA 更新
- 你需要搭建自己的更新服务器（而非使用 Expo 官方的 EAS Update 服务）
- 你希望深入理解 Expo 更新机制的底层工作原理
- 你在调试更新相关的网络请求问题

## 阅读前需要理解的背景知识

### 什么是 OTA 更新

在 Web 开发中，每次用户访问页面都会从服务器获取最新代码，这是默认行为。但在移动端应用中，代码被打包进 App 安装包（APK / IPA），通过应用商店分发。OTA 更新允许应用在启动时从服务器拉取最新的 JavaScript 代码和资源，而无需用户去应用商店下载新版本。

### 核心术语预览

| 术语 | 含义 |
|------|------|
| **Update（更新）** | 一组新的 manifest（清单）及其关联的 asset（资源文件），相当于 Web 中的一次部署 |
| **Manifest（清单）** | 描述一次更新的 JSON 文档，包含入口文件、资源列表、元数据等，类似于 Web 中的 `manifest.json` 或构建产物的索引文件 |
| **Directive（指令）** | 服务器告诉客户端执行某个特定动作（例如回滚）的指令，不是更新本身 |
| **Asset（资源）** | 更新中包含的文件，如 JavaScript bundle、图片、字体等 |
| **Runtime Version（运行时版本）** | 标识当前 App 原生代码的版本，用于确保 OTA 更新与原生层兼容 |
| **Launch Asset（启动资源）** | 更新的入口文件，通常是 JavaScript bundle，App 启动时加载它 |

### 协议与 HTTP 的关系

该协议建立在 HTTP 协议之上（遵循 RFC 7231 规范）。如果你熟悉 REST API 的请求-响应模型，理解这个协议会比较顺畅。它本质上就是一组约定好的 HTTP 请求头、响应头和响应体格式。

## 协议核心内容

### 一致性规则（Conformance Rules）

协议使用 RFC 2119 标准中的关键词来区分要求的严格程度：

- **MUST / MUST NOT**：必须 / 禁止，是强制要求
- **SHOULD / SHOULD NOT**：应该 / 不应该，是推荐做法，允许有合理例外
- **MAY**：可以，是可选项

客户端应忽略不认识的字段（向前兼容）。除非协议明确禁止，否则允许额外扩展功能。

> **对 Web 开发者的启示：** 这和 Web API 设计中的"宽松接收、严格发送"（Robustness Principle）原则一致。如果你自己实现更新服务器，对于不认识的字段直接跳过即可。

### 更新（Update）与指令（Directive）的区别

协议中服务器可以返回两种不同类型的响应内容：

1. **Update（更新）**：包含一个 manifest 和一组关联的 asset。客户端下载后会保存并在下次启动时使用。
2. **Directive（指令）**：告诉客户端执行某个特定操作，例如 `rollBackToEmbedded`（回滚到内嵌版本）。不包含新的代码或资源。

> **类比 Web 开发：** Update 类似于部署了一套新的前端代码；Directive 类似于服务器返回一个特殊指令告诉客户端"清除缓存，回退到上一个版本"。

## 客户端操作流程

客户端（即你的 Expo 应用）在运行时的行为如下：

### 1. 加载已保存的更新

应用启动时，会加载本地保存的最新更新。客户端可能根据 manifest 中的 metadata（元数据）进行过滤，选择匹配的更新。

### 2. 请求新更新

客户端向更新服务器发送 HTTP GET 请求，携带特定的请求头（详见下文）。

### 3. 处理响应

收到更新响应后，客户端会：
- 下载所有新的 asset
- 更新本地状态，包括 manifest filters（清单过滤器）和 server-defined headers（服务器自定义请求头）
- 将更新保存到本地

### 4. 处理指令

如果收到的是 directive 而非 update，客户端会消费并执行相应的指令动作。

## 请求规范（Request Specifications）

客户端向服务器请求更新时，必须发送 **GET 请求**，并携带以下请求头：

### 必需的请求头

| 请求头 | 说明 | 示例 / 备注 |
|--------|------|-------------|
| `expo-protocol-version` | 协议版本号，必须为 `1` | `1` |
| `expo-platform` | 目标平台标识 | `ios` 或 `android` |
| `expo-runtime-version` | 运行时版本字符串 | 标识当前 App 的原生代码版本 |

**平台标识的重要性：** 如果服务器不支持请求中指定的平台，应返回 HTTP 400（Bad Request）或 404（Not Found）错误。这类似于 Web 开发中根据 User-Agent 提供不同资源，但这里更加严格——平台不匹配直接报错。

**运行时版本（Runtime Version）：** 这是一个标识当前 App 原生层（native code）版本的字符串。OTA 更新只能推送 JavaScript 和资源层面的变更，如果原生代码发生了变化（比如添加了新的原生模块），就需要通过应用商店发布新版本，并更新 runtime version。服务器会根据这个值判断哪些更新适用于当前客户端。

### 之前收到的服务器自定义请求头

如果客户端在之前的响应中收到过 `expo-server-defined-headers`，后续请求必须将这些值一并发送。这是一个允许服务器在后续请求中获取自定义上下文的机制。

### 内容协商（Accept 头）

客户端可以通过标准的 HTTP `Accept` 头指定它能接受的响应格式：

- `application/json`：纯 JSON 格式
- `multipart/mixed`：多部分混合格式（可以在一个响应中包含 manifest、extensions 和 directive 多个部分）

支持可选的质量参数（quality parameters），这与 Web API 中的内容协商机制完全一致。

### 密码学验证请求头

如果客户端需要验证响应的签名，会发送一个 `Expect` 头，其值为一个结构化字典，包含：

- **signature**：签名值
- **keyid**：密钥标识符
- **algorithm**：使用的算法

这是告诉服务器"我需要你对响应进行签名，以便我验证其真实性"。

## 响应规范（Response Specifications）

### 响应格式

服务器必须以 JSON 或 multipart/mixed 格式返回响应。如果客户端请求的格式都不支持，服务器应返回 **HTTP 406（Not Acceptable）** 错误。

如果响应体为空的 multipart 响应，可以使用 **HTTP 204（No Content）** 状态码，表示"当前没有可用的更新"。

**服务器选择更新的逻辑：** 服务器必须选择满足请求中所有约束条件的**最新**更新。

### 标准响应头

| 响应头 | 说明 |
|--------|------|
| `expo-protocol-version` | 协议版本，值为 `1` |
| `expo-sfv-version` | 结构化字段值（Structured Field Values）的版本 |
| `expo-manifest-filters` | 清单过滤器，用于客户端在本地按 metadata 筛选已保存的更新 |
| `expo-server-defined-headers` | 服务器自定义请求头，客户端需在后续请求中携带这些值 |
| `cache-control` | 缓存控制，建议使用较短的缓存时间 |
| `content-type` | 内容类型，根据请求中的 Accept 头协商确定 |

> **注意 `cache-control` 的设置：** 协议建议使用较短的缓存时间。这与 Web 开发中 HTML 页面的缓存策略类似——你不希望客户端长时间缓存更新检查的结果，否则用户无法及时获取到新版本。

### 签名响应头（可选）

响应可以包含一个签名头，用于密码学验证，包含：
- 签名值（signature）
- 密钥标识符（keyid）
- 使用的算法（algorithm）

### Multipart 响应格式

当使用 `multipart/mixed` 格式时，响应中可以包含以下部分（parts），这些部分**无序排列**：

#### 1. Manifest 部分（清单）

JSON 格式，包含以下字段：

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | string | 全局唯一标识符（UUID），用于标识这次更新 |
| `createdAt` | string | ISO 8601 格式的创建时间戳 |
| `runtimeVersion` | string | 运行时版本，必须与客户端的运行时版本匹配 |
| `launchAsset` | object | 启动资源（入口文件），指向应用的 JavaScript 入口 |
| `assets` | array | 资源列表，每个资源包含 hash、key、mimeType、extension、url |
| `metadata` | object | 元数据字典，供 manifest filters 过滤使用 |
| `extra` | object | 额外数据，用于第三方配置（如项目 ID） |

**`launchAsset` 详解：** 这是更新的入口点，通常是打包后的 JavaScript bundle。类似于 Web 应用中 `index.html` 引用的主 JS 文件。客户端加载更新时，会从这个入口文件开始执行。

**`assets` 数组中的每个资源包含：**

| 字段 | 说明 |
|------|------|
| `hash` | 文件的 SHA256 哈希值，用于完整性验证 |
| `key` | 资源的唯一键 |
| `mimeType` | MIME 类型，如 `image/png`、`application/javascript` |
| `extension` | 文件扩展名 |
| `url` | 资源的下载地址 |

**`metadata` 的作用：** 允许更新携带标签信息（如版本号、渠道名等），客户端的 manifest filters 可以根据这些信息决定是否应用该更新。

**`extra` 字段：** 提供扩展点，例如可以包含项目 ID 等第三方配置信息。注意 manifest 中会忽略 extensions 部分的数据。

#### 2. Extensions 部分（扩展）

JSON 对象，将 asset 的 key 映射到自定义请求头的字典。当客户端下载特定 asset 时，需要携带这些自定义请求头。

> **实际用途举例：** 你可能需要在下载某些资源时携带认证 token 或 CDN 特定的请求头，extensions 机制允许你为每个资源单独指定这些头。

#### 3. Directive 部分（指令）

JSON 对象，包含：

| 字段 | 说明 |
|------|------|
| `type` | 指令类型，如 `rollBackToEmbedded` |
| `parameters` | 可选的参数对象 |
| `extra` | 可选的额外数据 |

**`rollBackToEmbedded` 指令：** 告诉客户端放弃当前的 OTA 更新，回滚到 App 安装包中内嵌的原始版本。这在 OTA 更新导致严重问题时非常有用——服务器可以指示所有受影响的客户端立即回滚。

> **类比 Web 开发：** 类似于你在 CDN 上发现了一个有问题的部署，然后通过配置让所有用户回退到上一个稳定版本。但在移动端，这个"回退"是通过协议级别的指令完成的。

**空响应：** 如果 multipart 响应中不包含任何部分，相当于一次空操作（no-op），客户端不做任何处理。

## 资源下载流程（Asset Fetching）

### 请求资源

客户端通过 **GET 请求** 下载 manifest 中列出的每个 asset URL，请求中需要携带：

- `Accept` 头
- `Accept-Encoding` 头（指定支持的压缩格式）
- extensions 中为该资源定义的自定义请求头（如果有）

### 资源响应要求

**资源必须是不可变的（immutable）：** 同一个 URL 必须始终返回相同的内容。这意味着如果更新了一个资源，必须生成一个新的 URL，而不是覆盖原有 URL 上的内容。

> **这与 Web 开发中的最佳实践一致：** 现代 Web 构建工具（如 Webpack、Vite）生成的文件名通常包含内容哈希（如 `app.a1b2c3d4.js`），本质上也是为了实现不可变资源。Expo 的更新协议将这个要求提升到了协议级别。

**客户端验证：** 客户端下载资源后，会计算文件的 SHA256 哈希值，并与 manifest 中声明的哈希值进行比较。如果不匹配，说明资源在下载过程中被篡改或损坏，客户端会拒绝该资源。

### 资源响应头

| 响应头 | 说明 |
|--------|------|
| `content-type` | 资源的 MIME 类型 |
| `content-encoding` | 编码方式（如 gzip、br） |
| `cache-control` | 建议使用较长的缓存时间（因为资源是不可变的） |

> **缓存策略对比：** manifest 的响应建议使用短缓存（因为需要频繁检查更新），而 asset 的响应建议使用长缓存（因为资源是不可变的，URL 变了内容才会变）。这与 Web 开发中对 HTML 文件用短缓存、对带哈希的静态资源用长缓存的策略完全一致。

### 数据压缩

协议推荐使用 **gzip** 和 **Brotli** 格式来压缩资源文件。这两种格式也是 Web 开发中最常用的传输压缩方案。

## 密码学签名（Cryptographic Signing）

### 签名的目的

协议支持对 manifest 和 directive 进行密码学签名。签名的核心目的是：

1. **确保更新来源可信：** 客户端可以验证更新确实来自你（或你信任的服务器），而非中间人攻击
2. **间接保护资源完整性：** 由于 manifest 中包含了每个资源的 SHA256 哈希值，对 manifest 签名后，只要 manifest 可信，其中引用的资源哈希也就可信了

> **类比 Web 开发：** 类似于 HTTPS 通过 TLS 证书保护传输层安全。但 Expo 的签名机制是在应用层进行的——即使 HTTP 传输被劫持，攻击者也无法伪造签名来推送恶意更新。

### 签名验证流程

1. 客户端在请求中通过 `Expect` 头表明需要签名验证
2. 服务器在响应中包含签名信息
3. 客户端使用**嵌入在 App 中或操作系统中的受信任根证书**来验证签名
4. **验证必须在处理更新之前完成** —— 如果签名验证失败，客户端不会应用该更新

### 传递性安全保障

签名的安全是传递性的：
- 签名保护 manifest → manifest 中包含资源的 SHA256 哈希 → 哈希保护资源

因此，只需要对 manifest 签名，就能间接确保所有关联资源的完整性。

## 注意事项、限制条件和坑点

### 协议版本固定为 1

请求头中的 `expo-protocol-version` 必须为 `1`。这不是 Expo SDK 版本，也不是 App 版本号，而是通信协议本身的版本号。

### 平台标识只支持 ios 和 android

`expo-platform` 目前只接受 `ios` 和 `android`。如果你尝试传入其他平台值，服务器应返回 400 或 404 错误。

### 运行时版本 ≠ 应用版本

Runtime version 标识的是原生代码的兼容性版本，不是用户可见的应用版本号（如 1.2.3）。两个不同的应用版本如果原生代码没有变化，可以共享同一个 runtime version。

### Multipart 部分是无序的

multipart 响应中的 manifest、extensions 和 directive 部分没有固定顺序。实现解析器时不能假设某个部分一定在前面。

### 资源不可变性是硬性要求

如果同一个 URL 返回了不同的内容，客户端的 SHA256 校验会失败，导致更新无法完成。在使用 CDN 或自建服务器时，务必确保这一点。

### 签名验证先于更新处理

客户端必须先验证签名，通过后才会处理更新。如果签名验证失败，整个更新会被丢弃。自建服务器时如果忘记签名或签名配置错误，更新将完全不生效。

## React Web 开发者需要特别注意的地方

### 1. 更新不是即时的

在 Web 开发中，部署后用户刷新页面就能看到新版本。但在 Expo 中，更新是在 App 启动时检查并下载的，用户需要重启 App 才能使用新更新（除非配合 `expo-updates` 的 API 实现热更新逻辑）。

### 2. 原生代码变更需要重新发版

Web 项目中你可以随时更改任何代码并部署。但在 Expo 中，如果修改了原生代码（添加了新的原生模块、更改了 App 配置等），必须通过应用商店重新发布，并更新 runtime version。OTA 更新只能推送 JavaScript 和资源层面的变更。

### 3. 没有"刷新"按钮

Web 用户遇到问题可以强制刷新（Ctrl+Shift+R），但移动用户没有这个操作。这就是 `rollBackToEmbedded` directive 存在的原因——它是移动端的"回退机制"。

### 4. 缓存策略的"两极分化"

Web 开发中的缓存策略通常比较灵活，可以对不同类型的资源设置不同的缓存时间。Expo 更新协议明确要求：manifest 短缓存，asset 长缓存。自建服务器时必须遵守这个原则，否则可能导致更新检查不及时或资源下载浪费带宽。

### 5. 签名不是可选的最佳实践

在 Web 开发中，HTTPS 基本是"标配"但技术上可以不用。在 Expo 更新协议中，如果你启用了签名验证（通过配置根证书），那么签名就变成了强制要求——未通过验证的更新会被直接丢弃。

## 实际开发建议

### 使用 EAS Update 而非自建服务器

除非你有特殊需求（如企业内部分发、合规要求），建议使用 Expo 官方的 EAS Update 服务。它已经完整实现了这个协议，你不需要关心 multipart 解析、签名管理等底层细节。

### 自建服务器的最低要求

如果必须自建更新服务器，需要实现：
1. 正确解析请求头（protocol version、platform、runtime version）
2. 返回正确格式的 manifest JSON
3. 确保资源的不可变性和 SHA256 一致性
4. 设置合理的 cache-control 头
5. 可选但推荐：实现密码学签名

### 调试更新问题时的检查清单

- 检查请求头是否正确发送（protocol version 是否为 1，platform 是否正确）
- 检查 runtime version 是否与服务器上配置的匹配
- 检查服务器返回的 content-type 是否与客户端 Accept 头匹配
- 检查资源的 SHA256 哈希是否与 manifest 中声明的一致
- 如果启用了签名，检查证书配置是否正确

## 总结

Expo Updates Protocol Version 1 定义了 Expo 应用获取 OTA 更新的完整通信规范。它建立在标准 HTTP 协议之上，通过精心设计的请求头、响应头和响应体格式，实现了安全、可靠的更新分发。

对于从 React Web 转向 Expo 的开发者来说，核心要点是：

1. **更新机制本质不同** —— 移动端是"拉取 + 本地缓存 + 下次启动生效"，而非 Web 的"即时加载"
2. **原生层和 JS 层分离** —— runtime version 是关键概念，它决定了哪些 OTA 更新可以被应用
3. **安全性是协议级别的** —— 通过签名和哈希验证，在协议层面保证了更新的完整性和来源可信
4. **资源管理遵循不可变原则** —— 与 Web 开发中带哈希的文件名策略理念相同，但在协议中被强制要求

理解这个协议有助于在 OTA 更新出现问题时进行有效排查，也能帮助你做出更明智的架构决策（比如是否需要自建更新服务器）。

---

## 文档导航

- **上一页**：[webview](./242__webview.md)
- **下一页**：[expo sfv 0](./244__expo-sfv-0.md)
