# Expo Structured Field Values（版本 0）

## 文档解决的问题

本文档描述了 Expo 平台对 HTTP 头部（Header）格式化标准的适配方案。

在 Web 开发中，HTTP 头部通常以简单的键值对字符串形式存在。但在移动端应用中，尤其是涉及 OTA（Over-The-Air）更新、网络请求等场景时，HTTP 头部可能需要承载更复杂、更结构化的数据。为解决这一问题，IETF 提出了 RFC 8941 标准来规范化 HTTP 头部的语法结构。Expo 基于该标准的一个子集实现了自己的版本，称为 **Expo Structured Field Values**（版本 0）。

> **对 React Web 开发者的说明：** 在 Web 开发中，你很少需要直接关心 HTTP 头部的结构化格式——浏览器和服务器框架通常帮你处理好了。但在 Expo/React Native 项目中，客户端（App）与服务端之间的通信（如检查更新、获取配置）可能依赖这些结构化头部来传递元信息。理解这个规范有助于你在调试网络请求或排查 OTA 更新问题时，能看懂 Expo 服务端返回的头部数据格式。

## 阅读前需要理解的背景知识

### 什么是 IETF RFC 8941

[IETF RFC 8941](https://tools.ietf.org/html/rfc8941)（Structured Field Values for HTTP）是一个互联网标准提案，目标是：

- **规范化 HTTP 头部语法**：让头部值不再只是随意格式的字符串，而是有明确的数据类型和解析规则
- **支持嵌套数据结构**：允许在头部中传递列表、字典等复杂结构，而不仅仅是简单的键值对

> **对 React Web 开发者的类比：** 可以把它理解为"HTTP 头部的 JSON 化"。就像 JSON 让 HTTP Body 有了结构化的标准格式一样，RFC 8941 想让 HTTP Header 也有类似的结构化能力。不过它比 JSON 更轻量、更受限，因为 HTTP 头部本身对长度和字符集有限制。

### 什么是 HTTP 结构化字段值

结构化字段值定义了以下几种数据类型（类似于编程语言中的基本类型）：

| 类型 | 说明 | 示例 |
|------|------|------|
| **String**（字符串） | 用双引号包裹的文本 | `"hello"` |
| **Integer**（整数） | 不带小数点的数字 | `42` |
| **Decimal**（小数） | 带小数点的数字 | `3.14` |
| **Dictionary**（字典） | 键值对的有序集合，类似 JavaScript 对象 | `key1=value1, key2=value2` |
| **List**（列表） | 有序的值序列 | `item1, item2, item3` |
| **Key values**（键值） | 字典中每个键对应的值 | 字典中的 `value1`、`value2` 等 |

> **注意：** 以上类型说明基于 RFC 8941 标准文档的通用定义。Expo 的版本 0 仅实现了其中的一个子集（见下文）。

## 核心内容：Expo 的实现范围

### 为什么是自定义版本

文档明确指出，由于 IETF RFC 8941 标准在文档发布时仍是一个 **进行中的工作**（work in progress），尚未完全定稿。因此，Expo 没有完整实现该协议，而是维护了一个 **自定义版本**，仅覆盖了标准的一个子集。

> **对 React Web 开发者的说明：** 这类似于你在项目中引入一个还在 Beta 阶段的第三方库——你不会直接使用其全部功能，而是只使用其中稳定的部分。Expo 采取同样的策略：只采用 RFC 8941 中已经稳定的部分。

### 版本 0 支持的子集

Expo Structured Field Values **版本 0** 仅支持以下 RFC 8941 协议内容：

#### 1. 所有键值（All key values）

支持字典（Dictionary）中所有键对应的值。这意味着你可以在结构化字段中使用完整的键值对映射。

#### 2. 字符串、整数和小数项（String, integer, and decimal items）

支持三种基本数据类型：

- **String**（字符串）：用于传递文本信息，如版本号、标识符等
- **Integer**（整数）：用于传递精确的整数值，如状态码、计数等
- **Decimal**（小数）：用于传递需要精度的数值

#### 3. 字典（Dictionaries）

支持字典这种数据结构。字典是一个有序的键值对集合，类似于 JavaScript 中的 `Map` 或普通对象。

### 版本 0 不支持的内容

基于文档内容进行推导，版本 0 **不支持**以下 RFC 8941 中定义的类型（基于文档中"仅实现以下子集"的表述）：

- **Lists**（列表）：有序的值序列
- **Inner Lists**（内部列表）：嵌套在列表或字典中的子列表
- **Byte Sequences**（字节序列）：二进制数据
- **Booleans**（布尔值）：真/假值
- **Tokens**（令牌）：不带引号的短标识符
- **Parameters**（参数）：附加在项目上的注解性键值对

> **开发影响：** 如果你在与 Expo 服务端交互时需要解析或构造结构化字段值，只能使用上述三种支持的类型。尝试使用列表或其他不支持的类型可能导致解析失败。

## 关键概念解释

### Structured Field Values（结构化字段值）

一种用于 HTTP 头部的数据格式标准。它让 HTTP 头部的值不再是自由格式的字符串，而是具有明确类型定义的结构化数据。在 Expo 的上下文中，这主要用于 Expo Updates 协议中客户端与服务端之间的通信头部。

### 与 Expo Updates 的关系

本文档位于 Expo 技术规范的 Reference（参考）分类下，与 [Expo Updates v1](./243__expo-updates-1.md) 规范并列。基于文档内容推导，结构化字段值主要被 Expo Updates 协议用于在 HTTP 头部中传递更新相关的元数据（如版本信息、平台标识等）。

### Version 0（版本 0）

表示这是 Expo 结构化字段值的初始版本。版本号为 0 通常意味着这是一个早期实现，后续版本可能会随着 RFC 8941 标准的演进扩展支持范围。

## 注意事项、限制条件和坑点

### 1. 子集限制

Expo 的实现是 RFC 8941 的一个 **受限子集**。如果你之前在其他项目中接触过完整的 RFC 8941 实现，不要假设所有类型都可用。在 Expo 环境中，只有字符串、整数、小数和字典是安全的。

### 2. 标准仍在演进

文档明确说明 RFC 8941 是一个进行中的标准。这意味着：

- 未来版本的 Expo 可能会扩展支持范围
- 当前文档中的信息可能在未来版本中发生变化
- 该文档最后修改日期为 **2021 年 5 月 24 日**，距今已有一段时间，标准本身可能已有更新

### 3. 自定义实现意味着兼容性差异

Expo 使用的是"自定义版本"（custom version），而非 RFC 8941 的标准实现。这意味着：

- 不能直接将其他平台的 RFC 8941 解析器用于 Expo 的结构化字段
- 如果你需要自己解析 Expo 返回的头部数据，需要按照 Expo 的子集来实现解析逻辑

## React Web 开发者需要特别注意的地方

### 1. HTTP 头部的角色不同

在 Web 开发中，HTTP 头部通常由浏览器和服务器框架自动处理，开发者很少需要直接操作头部的数据格式。但在 React Native / Expo 中，App 作为一个独立的客户端，可能需要直接解析服务端返回的自定义 HTTP 头部来获取更新信息。

### 2. 不要与 JSON 混淆

结构化字段值看起来可能有点像简化的 JSON，但它是一种完全不同的格式：

- JSON 支持嵌套对象和数组，结构化字段值的字典能力更有限
- JSON 有布尔值和 null，结构化字段值版本 0 不支持
- 结构化字段值的设计目标是在 HTTP 头部的有限空间内工作，因此更加精简

### 3. 这是底层协议规范

对于大多数 Expo 应用开发者来说，你不需要直接操作结构化字段值。Expo 的 SDK 和服务端会在底层处理这些细节。但当你遇到以下情况时，了解此规范会有帮助：

- 调试 Expo Updates 的网络请求
- 自定义更新服务端（self-hosted updates）
- 排查与 HTTP 头部相关的网络问题

## 实际开发建议

1. **一般开发者**：如果你只是使用 Expo 的标准服务（如 EAS Update），无需深入了解此规范的细节。Expo SDK 已经处理了所有结构化字段的解析和生成工作。

2. **自建更新服务的开发者**：如果你正在搭建自己的 Expo Updates 服务端（基于文档内容推导），你需要确保你的服务端在生成 HTTP 头部时，只使用版本 0 支持的类型（字符串、整数、小数、字典），不要使用列表或其他不支持的类型。

3. **调试场景**：当你在网络监控工具中看到 Expo 相关的 HTTP 请求带有看起来奇怪的结构化头部时，可以参照此规范来理解其格式和含义。

## 总结

本文档是一份简短的技术规范说明，核心信息如下：

- **Expo Structured Field Values 版本 0** 是 Expo 对 IETF RFC 8941（HTTP 结构化字段值）标准的受限实现
- 由于 RFC 8941 标准在发布时仍在演进，Expo 只实现了一个稳定子集
- 支持的内容：**键值（key values）**、**字符串/整数/小数（String, integer, and decimal items）**、**字典（Dictionaries）**
- 不支持 RFC 8941 中的列表、字节序列、布尔值等其他类型
- 该规范主要服务于 Expo Updates 协议中的 HTTP 头部通信
- 文档发布于 2021 年 5 月，属于 Expo 技术规范参考文档的一部分

> **整体评价：** 这是一份非常精简的规范文档，主要目的是声明 Expo 对 RFC 8941 的支持范围。对于大多数开发者而言，了解"Expo 使用了一种结构化的 HTTP 头部格式，且只支持有限的数据类型"这一核心信息即可。只有在涉及自建服务或深度调试时，才需要深入理解具体细节。

---

## 文档导航

- **上一页**：[expo updates 1](./243__expo-updates-1.md)
- **下一页**：[expo cli](./245__expo-cli.md)
